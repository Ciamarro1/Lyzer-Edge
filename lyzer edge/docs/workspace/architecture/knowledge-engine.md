# LACW — Living Knowledge Graph Architecture

## Overview
Knowledge inside Lyzer Edge is never static text. The `CognitiveKnowledgeEngine` manages living knowledge nodes with sources, confidence scores, relationships, validity windows, versioning, and half-life decay functions.
