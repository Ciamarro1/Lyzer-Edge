# LACW — Capability Composition Engine Specification

## Overview
Agents inside Lyzer Edge do not contain hardcoded logic; agents compose capabilities managed by the `CapabilityEngine`. Capabilities declare explicit contracts, versioning, dependencies, and permissions.
