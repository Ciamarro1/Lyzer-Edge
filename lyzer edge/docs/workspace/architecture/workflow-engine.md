# LACW — Declarative Cognitive Workflow Architecture

## Overview
The `CognitiveWorkflowEngine` manages observable, interruptible, resumeable, and parallelizable workflows. Workflows are declared as step pipelines with handlers and state context passing.
