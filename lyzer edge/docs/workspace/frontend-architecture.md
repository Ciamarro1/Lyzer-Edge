# LACW — Frontend SPA Architecture

## Overview
High-performance SPA built with vanilla JavaScript, ES modules, CSS design tokens, and decoupled SDK engines. Uses hash-based routing (`#workspace`, `#commandCenter`) and dynamic widget docking.
