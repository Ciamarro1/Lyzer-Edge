# Lyzer Edge — Strict Zero-Call Dead Code Inventory

## Verified Zero-Call Files & Un-instantiated Adapters

1. **`ExchangeExecutionLiveWS.js`**: Native WebSocket adapter for live exchange integration. Zero invocations in `SIMULATION` mode (Safe isolation).
2. **`PluginSandboxNativeProcess.js`**: Process kill fallback handler for native plugin memory breaches. Zero calls in standard operation.
3. **`SpatialAmbientInterfaceAdapter.js`**: AR/VR spatial computing interface placeholder adapter. Zero calls (Future spec).
4. **`DisasterRecoveryRegionSwitch.js`**: Automated cloud cross-region failover. Zero calls during local execution.
