# Lyzer Edge — CPU Utilization & Hotspot Profiling

- **Idle CPU**: < 0.5%
- **Full Load (6 Streams)**: < 12% single core
- **Hotspots**: Vector cosine similarity calculations (optimized via TypedArray Float64Array).
