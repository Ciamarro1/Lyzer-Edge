# Lyzer Edge — Quick Wins & Immediate Optimizations

1. Move temporary `scratch/*.py` scripts to `.gitignore` or `scripts/dev/`.
2. Add automated CI check verifying `npx vitest run` on every Git pull request.
